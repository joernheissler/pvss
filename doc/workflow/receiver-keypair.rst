.. _workflow.receiverkey:

Receiver key pair generation
=============================
As soon as there is need to reassemble the secret, the intended receiver of the secret
generates another keypair and shares the public key.
